# -*- coding: utf-8 -*-
import logging

# 配置日志
logging.basicConfig(
    filename='lockInfo.log',  # 指定日志文件的名称
    level=logging.INFO,    # 设置日志级别为INFO
    format='%(asctime)s - %(levelname)s - %(message)s'  # 指定日志记录的格式
)
